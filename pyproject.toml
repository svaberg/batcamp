[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "batcamp"
version = "0.1.0"
description = "Octree reconstruction and interpolation utilities"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [
  { name = "Dag Evensberget" },
]
keywords = ["octree", "interpolation", "amr", "batsrus", "simulation"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering",
]
dependencies = [
  "batread>=1.0.0",
  "numpy",
  "numba",
]

[project.urls]
Homepage = "https://github.com/svaberg/batcamp"
Repository = "https://github.com/svaberg/batcamp"
Issues = "https://github.com/svaberg/batcamp/issues"

[project.optional-dependencies]
tests = [
  "pytest",
  "pytest-cov",
  "pooch",
  "nbclient",
  "nbformat",
  "ipykernel",
  "matplotlib",
  "scipy",
  "vulture",
]
examples = [
  "scipy",
  "matplotlib",
  "pooch",
  "jupyter",
  "nbconvert",
  "ipykernel",
]

[tool.setuptools.packages.find]
include = ["batcamp*"]

[tool.pytest.ini_options]
addopts = "-m 'not slow and not pooch'"
pythonpath = ["tests"]
markers = [
  "slow: expensive tests excluded from the default unit-test run",
  "design_lockin: implementation-locking tests",
  "pooch: tests that fetch sample files via pooch/network",
]
