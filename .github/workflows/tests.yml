name: ci

on:
  pull_request:
  push:

env:
  XDG_CACHE_HOME: ${{ github.workspace }}/.cache

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          init-shell: bash

      - name: Install batcamp with test dependencies
        shell: bash -l {0}
        run: pip install -e '.[tests]'

      - name: Run flake8
        shell: bash -l {0}
        run: flake8 batcamp tests examples --count --statistics

  default:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          init-shell: bash

      - name: Install batcamp with test dependencies
        shell: bash -l {0}
        run: pip install -e '.[tests]'

      - name: Run default test suite with coverage
        shell: bash -l {0}
        run: pytest -q -m "not pooch and not perf" --cov=. --cov-report=xml:pytest-cobertura.xml

      - name: Codacy Coverage Reporter
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: codacy/codacy-coverage-reporter-action@v1.3.0
        with:
          project-token: ${{ secrets.CODACY_PROJECT_TOKEN }}
          coverage-reports: pytest-cobertura.xml

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          init-shell: bash

      - name: Install batcamp with test dependencies
        shell: bash -l {0}
        run: pip install -e '.[tests]'

      - name: Build distributions
        shell: bash -l {0}
        run: python -m build

      - name: Check built distributions
        shell: bash -l {0}
        run: python -m twine check dist/*

  perf:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Restore pooch cache
        uses: actions/cache@v4
        with:
          path: ${{ env.XDG_CACHE_HOME }}/pooch
          key: pooch-${{ runner.os }}-${{ hashFiles('examples/benchmark_helpers.py', 'tests/sample_data_helper.py') }}
          restore-keys: |
            pooch-${{ runner.os }}-

      - uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          init-shell: bash

      - name: Install batcamp with test dependencies
        shell: bash -l {0}
        run: pip install -e '.[tests]'

      - name: Run perf tests
        shell: bash -l {0}
        run: pytest -q -m perf -o addopts=''

  pooch:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Restore pooch cache
        uses: actions/cache@v4
        with:
          path: ${{ env.XDG_CACHE_HOME }}/pooch
          key: pooch-${{ runner.os }}-${{ hashFiles('examples/benchmark_helpers.py', 'tests/sample_data_helper.py') }}
          restore-keys: |
            pooch-${{ runner.os }}-

      - uses: mamba-org/setup-micromamba@v3
        with:
          environment-file: environment.yml
          cache-environment: true
          cache-downloads: true
          init-shell: bash

      - name: Install batcamp with test dependencies
        shell: bash -l {0}
        run: pip install -e '.[tests]'

      - name: Run pooch integration tests
        shell: bash -l {0}
        run: pytest -q -m pooch -o addopts=''
